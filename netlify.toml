[build]
  functions = "functions"
  publish = "client/build"
  command = "npm install && npm run build"

[dev]
  publish = "client/build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
